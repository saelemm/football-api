# football-domain

Coeur metier de l'application.

## Contenu

- Entites et objets valeur (`Team`, `Player`, `Transfer`, etc.)
- Regles metier et validations
- Exceptions metier
- Ports (`port.*`) et interfaces usecase (`usecase.*`)

Ce module ne depend pas des adapters techniques.

